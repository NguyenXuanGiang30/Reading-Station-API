# ============================================
# POSTGRESQL PROFILE CONFIGURATION
# ============================================
# Sử dụng cho production (Render.com)

spring.datasource.url=${DATABASE_URL:jdbc:postgresql://localhost:5432/tram_doc_db}
spring.datasource.username=${DATABASE_USERNAME:postgres}
spring.datasource.password=${DATABASE_PASSWORD:giang2005}
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA/Hibernate
# Note: Hibernate 6+ auto-detects dialect from JDBC URL, no need to specify

# Flyway (tắt vì dùng ddl-auto=update)
spring.flyway.enabled=false
